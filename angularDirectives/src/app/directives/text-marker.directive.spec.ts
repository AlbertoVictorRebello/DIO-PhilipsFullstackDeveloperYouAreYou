import { TextMarkerDirective } from './text-marker.directive';

describe('TextMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new TextMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
