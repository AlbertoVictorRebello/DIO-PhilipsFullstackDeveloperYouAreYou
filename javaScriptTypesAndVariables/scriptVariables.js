var firstName = "Alberto";
let lastName = "Rebello";

/* if (true) {
    var firstName = "otreblA";
    let lastName = "ollebeR"; // 

    console.log(`From a if-block, the variables firstName = ${ firstName} and lastName = ${ lastName }`);
}

console.log(`From a global scope, the variables  firstName = ${ firstName} and lastName = ${ lastName }`); */

if (true) {
    let firstName = "OTREBLA";
    let lastName = "OLLEBER"; // 

    console.log(`From a if-block again, the variables firstName = ${ firstName } and lastName = ${ lastName }`);
}

console.log(`From a global scope, the variables  firstName = ${ firstName } and lastName = ${ lastName }`);

lastName = 1010101
console.log(`From a global scope, the variable lastName was reasigned to another value type ${ lastName }`);

console.log(firstName[0]);;