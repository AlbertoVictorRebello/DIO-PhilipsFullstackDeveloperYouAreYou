function showAge(name, age) {
  return `${ name } is ${ age } years old.`  
}

function showCity(name, city) {
    return `${ name } lives at ${ city }.`  
  }

  function showHobby(name, hobby) {
    return `${ name } loves to practice ${ hobby }.`  
  }


  export {
      showAge,
      showCity,
      showHobby
  }

